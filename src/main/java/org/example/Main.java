package org.example;

//import javax.persistence.*;

public class Main {
    public static void main(String[] args) {

        //EntityManagerFactory emf = Persistence.createEntityManagerFactory("objectdb:productes.odb");
        //EntityManager em = emf.createEntityManager();

        //em.getTransaction().begin();
        //em.persist(new Alumne("12345678A", "Nom", "Cognom"));
        //em.getTransaction().commit();

        //TypedQuery<Alumne> query = em.createQuery("SELECT a FROM Alumne a", Alumne.class);
        //for (Alumne a : query.getResultList()) {
        //    System.out.println(s);
        //}

        //em.close();
        //emf.close();

    }
}